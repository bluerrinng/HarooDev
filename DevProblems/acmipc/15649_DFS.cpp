/*
15649번 문제를 dfs 방식으로 풀어본 것이다 

*/
#include <iostream>
#include <vector>

using namespace std;

void dfs(vector<int> arr1, int maxCount, vector<bool>visited)
{
    
}

int main()
{
    int num1, num2;

    cin >> num1 >> num2;

    vector<int> result (num1, 0);

    for(int i = 0; i < num1; i++)
    {
        result[i] = i+1;
    }

}